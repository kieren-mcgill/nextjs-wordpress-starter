
import React from 'react';

type DataType = {

};

interface PageTextProps {
    data: DataType;
}

const PageText: React.FC<PageTextProps> = ({ data }) => {
    return (
        <div>
        </div>
    );
}

export default PageText;