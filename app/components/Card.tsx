import React from 'react';

type DataType = {

};

interface CardProps {
    data: DataType;
}

const Card: React.FC<CardProps> = ({ data }) => {
    return (
        <div>
        </div>
    );
}

export default Card;